# yahoo-finance-history-news

## Usage

Clone the repository:

`git clone https://github.com/hqrrylyu/yahoo-finance-history-news.git`

Install dependencies:

`pipenv install`

Run:

`pipenv run python -m finance PD ZUO PINS`
